cd# A script which takes a list of names and assigns them to each other but never to themselves
# The goal is to also export the name to a .txt file named after the person and within the person they received

import csv # importing csv module
import sys #importing sys module for .txt creation

def name_file():                        # Function for creating new .txt name files
    file_name = 

names = csv.reader(file('names.txt'))   # Creating a list of names from the names.txt file

for item in names:                      # loop for 
    
    name = item
    
    
    

